# Отчет по тестированию интеграции `fullready_shop`

Дата: 2026-06-10

## Автоматические проверки

- `get_errors`:
  - `local/templates/fullready_shop/header.php` — ошибок не найдено.
  - `local/templates/fullready_shop/script.js` — ошибок не найдено.
  - `local/templates/fullready_shop/components/bitrix/catalog.section/fullready_cards/template.php` — ошибок не найдено.
- Синтаксис JS:
  - `node --check local/templates/fullready_shop/script.js` — успешно.

## Ручной чек-лист (требует запуска сайта)

- [ ] Главная: отображение блока карточек `fullready_cards`.
- [ ] Каталог: фильтр и список товаров работают без JS-ошибок.
- [ ] Карточка товара: цена/изображение/описание выводятся корректно.
- [ ] Корзина в шапке: отображается количество товаров.
- [ ] Внутренние страницы: хлебные крошки, `h1`, блок ссылок контентных разделов.
- [ ] SEO: в `<head>` присутствуют `canonical`, `og:type`, `og:title`.
- [ ] Адаптив: mobile/tablet/desktop на ключевых страницах.

## Известные ограничения

- Проверка `php -l` не выполнена: `php` CLI отсутствует в окружении.
- Полный E2E smoke-тест не выполнен: нет запущенного локального веб-окружения в рамках текущей сессии.

